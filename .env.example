# ── Mode / Payment switch ──────────────────────────
# test = no 402, returns 200 + price_preview
# live = enforce 402 paywall
TEOS_MODE=test
TEOS_REQUIRE_PAYMENT=0

# ── x402 Payment Config ──────────────────────────
X402_PAY_TO=0x6CB857A62f6a55239D67C6bD1A8ed5671605566D
X402_NETWORK=eip155:8453

# ── On-chain verification ─────────────
# 1 = verify receipt + confirmations + USDC Transfer
# 0 = header-only (dev). NOTE: If TEOS_REQUIRE_PAYMENT=0, gate is bypassed anyway.
X402_VERIFY_ONCHAIN=0
X402_CONFIRMATIONS=2
RPC_URL_BASE=https://mainnet.base.org

# ── USDC contract override (optional) ───────────────
USDC_ADDRESS=0x833589fCD6eDb6E08f4c7C32D4f71b54bdA02913

# ── Pricing (USDC) ───────────────────────────────
PRICE_BASIC=0.25
PRICE_PREMIUM=0.50
PRICE_PIPELINE=1.00

# ── Server ────────────────────────────────────────
PORT=8000
HOST=0.0.0.0
